name: Update YouTube Videos

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch: {}

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch latest videos
        env:
          YT_API_KEY: ${{ secrets.YT_API_KEY }}
        run: |
          mkdir -p data
          curl -s "https://www.googleapis.com/youtube/v3/search?key=$YT_API_KEY&channelId=UCbkas9iM-0U9JQ-ag4J4jTg&part=snippet&order=date&maxResults=10&type=video&videoDuration=long" \
          > data/videos.json

      - name: Commit updated data
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add data/videos.json
          git diff --staged --quiet || (git commit -m "Update videos.json" && git push)
